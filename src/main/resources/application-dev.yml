spring:
  jpa:
    # Local development: log every SQL statement (and the generated Hibernate SQL).
    # Activate with: --spring.profiles.active=dev (or SPRING_PROFILES_ACTIVE=dev).
    show-sql: true
    properties:
      hibernate:
        format_sql: true